#pragma once

#include <algorithm>
#include <limits>

enum class Solutions { ZERO, ONE, INF };

const double PI{ atan(1) * 4 };
const int INF{ std::numeric_limits<int>::max() };
const double INF_D{ std::numeric_limits<double>::max() };